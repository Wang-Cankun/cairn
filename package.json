{
  "name": "cairn",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Local claim-graph store: plain-text claims in git, CLI as sole writer, derived SQLite index, portable published snapshots.",
  "bin": {
    "cairn": "src/cli.ts"
  },
  "scripts": {
    "cairn": "bun run src/cli.ts",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  }
}
