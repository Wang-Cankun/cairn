# Findings — treatment scoring analysis

Shared write-up the team reads. Each finding that lands here is supposed to carry the
claim id that grounds it; a bare finding with no claim id is laundered — it has lost the
evidence, fork, and verification scars that Cairn keeps attached.

## Step 07 — scoring

We found that treatment samples score markedly higher than control samples.

## Modelling

The results show the logistic-regression model separates the two groups with high AUC.

## Housekeeping

Re-run the pipeline before the next sync; remember to refresh claims afterwards.
